# 修图台 · 版本说明

## 当前版本：v2.3.0（versionCode 16）

安装包位置：`/sdcard/Download/修图台.apk`（同一份也存为 `修图台-v2.3.0.apk`）

---

## 本次：工程规范（代码仓库化）

这一版**没有改功能**，补齐的是"成熟项目"的基础设施。

### 新增文件

| 文件 | 作用 |
|---|---|
| `README.md` | 项目说明、快速开始、构建步骤、架构解释、常见问题 |
| `CONTRIBUTING.md` | 贡献指南：代码约定、测试要求、踩坑记录 |
| `CHANGELOG.md` | 更新日志（从 version.json 生成，与版本号同步） |
| `LICENSE` | MIT 授权 |
| `.gitignore` | 忽略构建产物与**签名密钥** |
| `package.json` | 统一命令：`npm test` / `npm run build` / `npm run serve` |
| `tools/lint.js` | 轻量代码检查（零依赖） |
| `.github/workflows/ci.yml` | 提交自动跑检查与测试 |

### 建立了 Git 仓库

3 次提交，44 个文件纳入版本管理：

```
f541c6f 补充项目文档：CHANGELOG 与 CONTRIBUTING
e127f6a 工程规范：README / LICENSE / .gitignore / package.json / lint / CI
12aab6c 初始提交：修图台 v2.3.0
```

**关键：签名密钥（keystore.jks）已被忽略。** 这个文件泄露意味着他人能伪造你的更新包，所以我在 `.gitignore` 里显式排除，并在 lint 里加了检查。

### lint 检查的是"真实踩过的坑"

不是抄一份通用规则，而是把本项目历史上出过的问题做成检查项：

| 检查项 | 对应真实事故 |
|---|---|
| 源码混入 HTML 结构 | 构建时 `$'` 把 `</body></html>` 注入进 JS 字符串 |
| 重复函数定义 | 测试块被重复插入（一次插了 16 份） |
| 硬编码密钥 | 防止误把 Key 写进代码 |
| 版本号不一致 | version.json / version.js / package.json 必须同步 |
| 单文件产物结构 | `</body>` 必须恰好 1 次，内联脚本必须语法正确 |
| 调试代码残留 | `process.env.DB` 之类的临时诊断 |

**反向验证过**：故意注入这五类问题，lint 都能抓到。

### CI 配置

- **每次提交**：lint → 单元测试 → 回归测试 → 端到端测试 → 构建单文件 → 上传产物
- **打标签时**：额外构建 APK

端到端测试依赖 jsdom 与 @napi-rs/canvas（可选依赖，避免影响零依赖的构建流程），
CI 里装不上就跳过，不影响其他检查。

### 验证方式

我把仓库**全新克隆到一个空目录**，模拟 CI 环境跑完整流程：

```
1. lint              → 检查通过
2. 单元测试          → 80 passed
3. 回归测试          → 387 passed
4. 端到端测试        → 232 passed
5. 构建单文件        → 成功（278.9 KB）
6. 确认密钥未泄漏    → keystore.jks 不存在 ✓
```

---

## 上次（v2.3.0）：成本预估 —— 点之前就知道花多少钱

### 为什么需要

生图是按张收费的，但**一次点击可能产生多次调用**：

| 选区尺寸 | 调用次数 | 成本（Qwen-Image-Edit） |
|---|---|---|
| 800×600 | 1 次 | $0.04（¥0.28） |
| 1500×1200 | 2 次 | $0.08（¥0.57） |
| 3000×2000 | 6 次 | $0.24（¥1.70） |
| 4000×3000 | **9 次** | **$0.36（¥2.56）** |

选区超过 1400px 会自动分块，每块各调用一次模型 —— **成本随块数翻倍**。
之前界面上完全看不出这一点，容易误操作。

### 现在

**1. 生成前显示预估**
按钮下方直接写「预计 $0.040（¥0.28）」，分块时额外标注「6 次调用」。

**2. 大额自动确认**
预估超过 **$0.2**（约 ¥1.4）时先弹确认，并说明原因：

```
这次生成预计花费 $0.360（¥2.56）

原因：选区较大，需要分 9 块分别处理，共 9 次模型调用。

要继续吗？
（缩小选区可以减少调用次数）
```

**3. 会话累计花费**
模型按钮上显示「已花 $0.12」，随时知道这次修图总共花了多少。

**4. 可自定义单价与汇率**
- 未收录的模型可以手动填单价
- 中转/代理价格不同也能自己改
- 汇率可调（默认 7.1）

### 价格来源（都是官方页面，不是猜的）

| 模型 | 单价 | 来源 |
|---|---|---|
| Qwen-Image-Edit | $0.04/张 | 硅基流动官方博客 |
| FLUX.1-Kontext-pro | $0.04/张 | 硅基流动官方博客 |
| FLUX.1-Kontext-max | $0.08/张 | 硅基流动官方博客 |
| FLUX.2-pro | $0.03/张 | 硅基流动定价页 |
| Z-Image-Turbo | $0.005/张 | 硅基流动定价页 |
| gpt-image-1 | $0.042/张（medium） | OpenAI 官方模型页 |

**未收录的模型显示「未知」，不会编造数字。**

### 修复的一个真实 bug

写测试时发现：**空估算被当成 1 次调用**，导致花费统计算多。
（`num(e.calls, 1)` 默认值写成了 1，应该是 0）

### 端到端实测

- 小选区显示「预计 $0.040（¥0.28）」✓
- 分块时显示「9 次调用」且金额按次数累加 ✓
- 生成后累计花费增加且与单价一致 ✓
- 未知单价显示「未知」，填了自定义单价后能预估 ✓
- 大额时弹出确认，取消则不生成 ✓

**全套测试 699 项全绿**（80 单元 + 387 回归 + 232 端到端）。

---

### 顺带修掉一个隐蔽的构建 bug

构建单文件版时报「仍有外部引用」，排查发现是 JavaScript 的经典陷阱：

替换串里的 **`$'`** 是特殊标记（意思是"匹配之后的文本"），
而我的价格提示里有 `'…单价：$'` 这样的字符串，导致 `</body></html>`
被注入进了 JS 代码中间 —— **源码是对的，只有产物坏掉**，非常难查。

已改用函数形式替换，并加了**构建守卫**：产物里 `</body>` 出现次数必须为 1，
且每个内联脚本块都要通过语法检查。反向验证过：故意改回错误写法时守卫立刻拦下。

---

## 上次（v2.2.0）：导出预设 —— 按交付场景一键选择

### 为什么需要

同一个修图结果，交付场景不同，要求完全相反：

| 场景 | 需要 |
|---|---|
| 客户交付 | 全尺寸、最高画质、**保留**拍摄信息 |
| 发朋友圈 | 压到长边 2000、**去掉定位**（别暴露家在哪） |
| 发小红书 | 长边 1440、画质与体积均衡 |
| 印刷/后期 | PNG 无损、保留全部元数据 |
| 网页预览 | 长边 1600、体积优先、不要元数据 |

之前只有「JPEG/PNG + 质量滑块」，每种场景都要手动调。

### 现在：6 个预设

| 预设 | 尺寸 | 格式 | 拍摄信息 | 定位 |
|---|---|---|---|---|
| **原尺寸交付**（客户/后期） | 原尺寸 | JPEG q95 | 保留 | 保留 |
| **无损交接**（印刷/精修） | 原尺寸 | PNG | 保留 | 保留 |
| **微信 / 朋友圈** | 长边 2000 | JPEG q85 | 保留 | **移除** |
| **小红书 / 微博** | 长边 1440 | JPEG q88 | 保留 | **移除** |
| **网页 / 预览** | 长边 1600 | JPEG q80 | 不保留 | — |
| 自定义 | 手动 | 手动 | 手动 | 手动 |

### 几个细节处理

**1. 只缩不放**
小图不会被拉大（避免模糊）。比如 800×600 的照片选「微信」预设，仍是 800×600。

**2. 长边是「宽高中较大的一边」**
竖图按高度缩放。3000×6000 选「小红书」→ 720×1440。

**3. 移除定位但保留其他信息**
GPS 移除是**精准操作**：只清掉 GPS 指针，相机型号、镜头、光圈、快门、ISO、拍摄时间全部保留。
（做法是把 GPS IFD 指针清零，而不是重建整个 EXIF —— 后者风险高得多）

**4. 设置里实时显示最终尺寸**
选预设时直接显示「→ 导出 2000×1333（已缩小）」，不用猜。

### 端到端实测

用一张 3000×2000、带 GPS 的 JPEG 走完整导出流程：

- 微信预设 → 实际导出 **2000×1333**，比例保持，**GPS 已移除**，拍摄信息保留 ✓
- 原尺寸预设 → 实际导出 **3000×2000**，未缩放 ✓
- 网页预设 → 实际导出 **1600×1067**，**无拍摄信息** ✓

**全套测试 645 项全绿**（80 单元 + 350 回归 + 215 端到端）。

---

## 上次（v2.1.0）：统一撤销 —— 所有操作都能撤销了

### 之前的局限

撤销只覆盖「生成结果」，其它操作都不进历史：

| 操作 | 之前 | 现在 |
|---|---|---|
| 画笔画错一笔 | 只能「清空笔迹」**全部重画** | **逐笔撤销** |
| 删错了一条修改记录 | 无法恢复 | **撤销恢复**（插回原位置，顺序不乱） |
| 强度/羽化调过头 | 无法还原 | **撤销恢复** |
| 误关某条修改 | 无法还原 | **撤销恢复** |

### 实现要点

**1. 命令式撤销栈，只存差异不存快照**

如果每步都存一张合成图快照，3072×2048 一步就是 24MB，100 步 = 2.4GB。
所以只记录「反向操作需要的最小数据」：

- 删除图层 → 记索引 + 引用（内容本身已存在，不复制）
- 调参 → 记 `before` / `after` 两个数值
- 画笔 → 记笔迹对象

100 步历史几乎不占额外内存。

**2. 拖动滑块只在「松手」时记一条**

拖动过程中实时预览，但松手才写历史 —— 否则拖一次会产生上百条，
把撤销栈冲爆。

**3. 未知命令类型一律拒绝**

避免历史里出现「撤销不掉」的条目（测试时发现的边界问题）。

### 测试中发现并修复的真实 bug

写测试时断言失败：`撤销调参后强度为 1 → 实际 100`。

排查发现是**真 bug**：滑块值是 0~100，而图层内部存的是 0~1（比例）。
撤销命令记的是**滑块原始值**，撤销时会把 `100` 写进 `opacity` 字段（越界）。

虽然 `normalizeLayer` 会兜住（夹取到 1），但羽化等参数就会被写错值。
**已修复**：记录时统一转成内部值。

这类 bug 靠肉眼审查很难发现，是端到端测试抓出来的。

### 验证（端到端实测）

- 画笔 → 撤销 → 笔迹消失 → 重做 → 恢复 ✓
- 清空笔迹 → 撤销 → 笔迹回来 ✓
- 生成 → 撤销 → 图层移除且画面复原 → 重做 → 效果回来 ✓
- 删除记录 → 撤销 → 图层插回**原索引**（顺序正确）✓
- 关闭图层 → 撤销 → 效果回来 ✓
- 调强度到 20% → 撤销 → 恢复 → 重做 → 回到 20% ✓
- 连续撤销 30 次不报错、状态合法 ✓

**全套测试 598 项全绿**（80 单元 + 315 回归 + 203 端到端）。

---

## 上次（v2.0.0）：非破坏性编辑 —— 修改记录面板

### 这是最重要的一次升级

之前每次生成的结果会直接「烘焙」进照片。带来三个限制：

- 想微调上一次的羽化强度 → 必须撤销重做
- 生成效果太强想减弱 → 只能重新生成（再花钱、再等待）
- 想对比「改之前/改之后」→ 只能靠撤销

### 现在：每次生成都是一条可独立调整的记录

点工具栏的 **「修改记录」** 打开面板，每次生成对应一条，可以：

| 操作 | 说明 |
|---|---|
| **调整「效果强度」** | 0~100%。觉得模型改太狠？拖到 50% 就能减弱，**不用重新生成** |
| **调整「边缘羽化」** | 随时改接缝柔和程度 |
| **调整「色彩匹配」** | 随时改色调对齐强度 |
| **临时关闭** | 一键对比改之前/之后，再一键恢复 |
| **删除** | 删掉中间任意一次修改（不只是最近一次） |

每条记录还显示**选区尺寸**和**覆盖比例**，一眼看出这张图改了多少。

### 为什么能做到「不重新生成」

因为羽化、色彩匹配、强度这些参数是在**合成时**应用的，不是烧进生成结果里的。
所以拖动滑块只是重新合成一遍（毫秒级），**完全不调用模型**。

### 实测验证（端到端）

- 不透明度 0% → 画面完全恢复原图 ✓
- 不透明度 50% → 半强度混合 `[238,148,148]` ✓
- 临时关闭图层 → 恢复原图 ✓
- 调整羽化 → 边缘过渡变化 ✓
- 删除记录 → 完全恢复 ✓
- **以上全部操作，模型调用次数 = 0** ✓

还验证了：画笔排除的区域，**无论不透明度调到多少都严格为 0**（不会被误改）。

### 完整功能列表（截至 v2.0.0）

**核心**：自由框选 → 生成 → 自动贴回原位置
**非破坏性**：修改记录面板，可调强度/羽化/色彩匹配、可开关、可删除
**画质**：边缘羽化、接缝色彩匹配、细长选区不变形、大选区自动分块无缝拼接
**元数据**：导出保留 EXIF 拍摄信息与 ICC 色彩配置
**稳定**：编辑历史内存上限、编辑进度自动保存恢复
**兼容**：自动检测可用模型、多服务商（硅基流动 / OpenAI / 自定义）、失败原因诊断

**全套测试 540 项全绿**（80 单元 + 279 回归 + 181 端到端）。

---

## 上次（v1.9.0）：内存上限 + 编辑进度自动保存

### 解决的问题

**问题 1：编辑次数多了会崩**

3072×2048 的图，每次编辑要存 24MB 的 patch。20 次编辑就是 **480MB** ——
手机上必然被系统杀掉。实测数据：

| 编辑次数 | 内存占用 |
|---|---|
| 5 次 | 120MB |
| 10 次 | 240MB |
| 20 次 | **480MB** ← 危险 |
| 30 次 | 720MB |

**修复**：加了内存预算（默认 192MB，可调 64~512MB），超出时按两级策略处理：
1. 先把**较早**的 patch 降采样保存（内存降到 1/4），**最近 3 次保持原样**（最可能回退的是最近操作）
2. 仍超出才丢弃最老的编辑，并在界面上明确告知

实测：20 次编辑（480MB）→ 自动降到预算内；40 次编辑 → 降采样 37 条 + 丢弃 17 条，仍可正常撤销重做。

**问题 2：切出去接个电话，工作全丢**

Android 后台回收很常见。之前只存了配置，编辑进度完全丢失。

**修复**：自动保存编辑进度（含编辑记录、画笔范围、原图），重开后提示
「发现未完成的编辑 · N 处修改 · X 分钟前」，可选择恢复或放弃。

- 画笔掩膜做了**量化压缩**，存储体积降到约 **7%**，往返误差 < 0.005（肉眼不可见）
- 换图时自动清掉旧会话，不会串到别的照片
- 只保留最近能放下的若干条（优先保住当前工作）

### 新增设置项

| 设置 | 默认 | 说明 |
|---|---|---|
| 编辑历史内存上限 | 192 MB | 可调 64~512MB |
| 自动保存编辑进度 | 开 | 应用被回收后可恢复 |

### 验证

新增 21 项专项测试：内存规划（预算内/降采样/丢弃/4K 图/边界）、
掩膜压缩往返、会话规划（体积限制/时间顺序/保留最新）、
以及端到端「6 次真实编辑 → 会话写入 → 撤销重做 → 基准图还原」。

**全套测试 492 项全绿**（80 单元 + 254 回归 + 158 端到端）。

---

## 上次（v1.8.0）：保留拍摄信息与色彩配置

### 为什么重要

canvas 重绘会把照片的元数据**全部丢掉**。对摄影师来说这意味着：

- 相机型号、镜头、光圈、快门、ISO、拍摄时间、GPS 全没了 → 平台/客户要元数据时无法交片
- ICC 色彩配置丢失 → 广色域照片会出现偏色

### 修复内容

| 项目 | 之前 | 现在 |
|---|---|---|
| EXIF 拍摄信息 | 导出后全部丢失 | **完整保留**（相机/镜头/光圈/快门/ISO/时间/GPS） |
| 旋转标记 | 可能被系统二次旋转 | 归一化为 1，**不会二次旋转** |
| ICC 色彩配置 | 丢失 | sRGB 原样保留；广色域按 sRGB 导出并提示 |
| 界面提示 | 无 | 顶栏显示「含拍摄信息 / sRGB / 广色域 / 已校正旋转」 |

### 一个容易踩的坑（已处理）

手机竖拍的照片带 `Orientation=6` 标记。浏览器解码时会**自动把像素转正**，
但如果把 `Orientation=6` 原样写回，系统会**再转一次** → 照片躺倒。

处理方式：写回前把 Orientation 归一化为 `1`，并保留其余所有字段。
实测验证：`Orientation=6` 的 600×400 原图 → 导出 400×600（已转正）且标记为 1，不会二次旋转。

### 广色域的处理说明

canvas 会把像素转换到 sRGB。如果原图是 Adobe RGB / ProPhoto / Display P3，
**写回原配置反而会让颜色过饱和出错**。所以遇到广色域照片时：
- 按 sRGB 导出（颜色正确）
- 界面上明确提示「已按 sRGB 导出」

如果你需要保留广色域工作流，告诉我，我可以加「保持原色彩空间」选项
（需要改用 ImageBitmap 的 colorSpace 选项重新渲染，改动较大）。

### 验证

新增 23 项元数据专项测试：EXIF 读写、Orientation 归一化、
ICC 分片无损往返（含 15 万字节的大配置跨 3 段）、超大 EXIF 保护、sRGB 识别、非 JPEG 安全跳过。

**全套测试 454 项全绿**（80 单元 + 233 回归 + 141 端到端）。

---

## 上次（v1.7.0）：画笔导致图片偏蓝 —— 已修复

### 问题

用画笔后图片带上一层淡蓝，生成结果也带蓝色，和原图完全不契合。

### 根因（两个叠加的错误）

**错误 1：把蓝色标记画进了发给模型的图片**

我原本的做法是：把「要修改的区域」涂成半透明蓝色发给模型当提示。但模型**不认识这种提示**——
它把蓝色当成了画面内容的一部分，于是生成结果也带着蓝色。

**错误 2：预览层默认把整个选区都蒙上蓝色**

笔刷默认模式是「排除保护」，掩膜初始值全为 1（整块都改）。预览逻辑却按
「mask=1 就涂蓝」来画 —— 结果**没有任何笔迹时，整个选区都是蓝的**，
用户自然会以为照片变色了。

### 修复

| 项目 | 之前 | 现在 |
|---|---|---|
| 发给模型的图片 | 叠加半透明蓝色标记 | **原图原样发送，零标记** |
| 告诉模型改哪里 | 靠蓝色标记 | 靠文字描述（「请修改画面中央约 81% 的区域」，比例按实际几何算出） |
| 屏幕预览 | 整个选区涂蓝 | **只在「画笔排除保护」的区域显示灰色**，无笔迹时不显示任何蒙层 |

### 验证方式（像素级）

我构造了中性灰底图 + 画笔涂抹，然后**解码实际发给模型的图片**逐像素统计：
- 修复前：蓝色偏高的像素占 **82~88%**
- 修复后：蓝色偏高的像素 **0%**

还做了一个反向验证：**故意把蓝色叠加加回代码**，测试立刻失败（82% 蓝偏）——
证明这个回归测试真的能防住问题复发。

---

## 上次（v1.6.0）：真正的根因 —— 图片发送格式错了

### 找到根因了

反复出现 `upstream_text_reply`，最终定位到一个**应用本身的 bug**：

- OpenAI 系的图像**编辑**接口要求用 **multipart 表单上传**（图片作为文件字段），
  也就是 `POST /v1/images/edits`
- 而我之前一直用 **JSON** 打 `POST /v1/images/generations` —— 这个接口只认文字 prompt，
  **不认 JSON 里的 image 字段**，图片在服务端被静默丢弃
- 于是上游只看到你快 1000 字的修改要求，就回你一段"请上传照片"的文字 ——
  正是你看到的那段 `upstream_text_reply`

### bug 出在哪

发送时有一行代码把请求的 Content-Type 拼错了：

```
之前：Content-Type = {JSON} ← 覆盖了 multipart
现在：Content-Type = {multipart/form-data; boundary=...} ← 正确
```

multipart 二进制体其实发出去了，但声明自己是 JSON，上游解析失败 → 图片丢失。

### 修复与自查

1. OpenAI 编辑请求现在**正确走 multipart**，图片作为文件字段上传，逐字节验证无损
2. 报错面板 →「查看本次请求」现在会显示：
   - 发送格式（multipart 表单 / JSON）
   - 是否带图、图片大小
   - 出图尺寸、小图放大倍数
   一眼就能看出问题出在哪一环

### 仍需确认

如果你用的是**正规 OpenAI 接口**（api.openai.com）或标准 OpenAI 兼容中转，
现在应该能正常出图了。如果**仍然**报 `upstream_text_reply`，那说明你的中转
在其服务端就把请求转成了对话模型 —— 这种只能换中转或换模型名，应用层已无能为力。

---

## 上次（v1.5.0）：小选区被上游拒收 —— 已解决

**问题**：很多接口对输入图有**硬性最小尺寸**（OpenAI 类要求总像素 ≥ 655,360，约 0.66MP，
且宽高必须是 16 的倍数）。摄影师常改的恰恰是**小区域**（一颗痣、一处瑕疵、一行字），
框选 100×100 裁出来只有 1 万像素，**直接发过去必被拒收**。

### 解决方式：小选区自动放大后发送

| 步骤 | 处理 |
|---|---|
| 1. 框选 | 100×100 的小区域（原样保留，**不改写你的选区**） |
| 2. 外扩 | 加上周边上下文 → 124×124 |
| 3. **放大** | 按需放大到 **832×832**（6.7 倍），满足最小像素与 16 倍数要求 |
| 4. 发送 | 模型看到的是合规大图，能正常处理 |
| 5. 贴回 | 按**原分辨率**精确缩回 100×100 贴到原位置 |
| 6. 导出 | 在原图分辨率上重新合成，清晰度由原图决定 |

### 三个关键设计

1. **只放大选区，不放大整张照片** —— 省内存，也避免大图被接口的边长上限拒绝
2. **贴回时保留模型输出的高分辨率** —— patch 以放大后的分辨率保存，
   到最后一刻才缩到选区大小，避免"先缩小再放大"糊两次
3. **可开关** —— 设置里新增「小选区自动放大后发送」。
   如果你的接口没有最小尺寸限制，或发现放大后画质下降，可以关掉

### 实测验证
- 60×60 到 512×512 的小选区，放大后**全部达标**
- 放大后**比例保持**（不会把方形拉成长方形）
- 贴回位置**无偏移**（选区中心对齐，误差 < 2 像素）
- 贴回后**选区外零像素改动**

---

## 上次（v1.4.0）：上游返回文字（upstream_text_reply）

**您给的原始信息里有决定性线索：**

```json
{"error":{"code":"upstream_text_reply","message":"请上传需要编辑的原始照片…我会仅修改蓝色标记区域内的物体…"}}
```

`upstream_text_reply` 的含义是：**上游回了一段文字，而不是图片**。
而且这段文字的内容，正是「复述你的要求 + 用助手口吻作答」——
这是**对话模型**的典型行为，不是生图模型。

### 根因：接口/模型配的是对话模型

生图模型收到请求会返回图片；对话模型只会回一段话。
所以问题不在选区、不在图片大小，而在**当前配置的模型不是生图模型**。

### 修复了什么

1. **诊断纠错**（最关键）
   原来把这段错误误判成「没收到图片」，还建议你「换模型、把选区拉大」——
   方向完全错了。现在明确识别为「上游返回文字」，直接指出是模型/接口配错，
   并引导去「自动检测可用模型」。

2. **生成前预检**
   如果你填的模型名看起来是对话模型（如 `deepseek-chat`、`gpt-4o`、`claude-3-5-sonnet`），
   点生成时会**先弹提示**，而不是白等一次失败。仍提供「仍要尝试」按钮，
   以防某些中转的命名不按常规。

3. **新增「查看本次请求」**
   报错面板里可以展开看**实际发出的请求**：接口地址、模型名、
   **是否带了图片（以及图片多大）**、出图尺寸、请求字段列表。
   这样能一眼确认问题出在"没带图"还是"模型不对"，不用再猜。

### 怎么解决（操作步骤）

1. 打开应用 → 右上角「设置」
2. 确认「接口地址」和「API Key」填对
3. 点 **「自动检测可用模型」**
4. 从结果里选一个标着 **「可编辑」** 的模型（推荐 `Qwen/Qwen-Image-Edit`）
5. 回主界面重新生成

> 注意：模型名必须是**图像编辑**模型。像 `deepseek-chat`、`gpt-4o`、`qwen-plus`
> 这类是对话模型，无论怎么调都只会回文字。

---

## 上次（v1.3.0）：GPT Image 系列尺寸被拒的问题

**你指出的问题在我这边确实存在，已修复三处。**

### 1. 尺寸不合规，请求在服务端就被拒了
GPT Image 系列（gpt-image-1 / GPT Image 2.5）对出图尺寸有硬性约束：
总像素 ≥ 655,360、宽高均为 16 的倍数、单边 ≤ 3840、宽高比 1:3 ~ 3:1。

我原来的内置尺寸表里带着 **512×512、576×1024、1024×576** 这三个尺寸 ——
它们只有 26 万 ~ 59 万像素，**低于下限，发过去 100% 被拒**。
现已按规范修正，并新增自动校验：挑选尺寸时先剔除不合规项，
发送前再兜一道修正（512×512 → 816×816，自动补足像素并保持比例）。

### 2. 尺寸错误被误判成「没收到图片」
你说的"检测为空"很可能就是这里 —— 我原来的诊断逻辑把带 `image` 字样的
尺寸错误归到了「缺图」分支，把人引向错误方向（去换模型、调选区），
但真正原因是尺寸参数不合法。

现已把尺寸判断提到缺图判断**之前**，并单独归类为「出图尺寸不符合接口要求」，
原始报错里服务商给出的具体限制会一并显示。

### 3. 新增 GPT Image 2.5 条目
模型列表里补上了 `gpt-image-2`，尺寸约束与 gpt-image-1 一致。
另外用户手填的模型名不在内置表里时，也会按名字推断能力，不再只认内置列表。

### 顺带
- 生图前会显示**本次实际使用的出图尺寸**，不再"闷头发出去"
- 硅基流动等不受 OpenAI 约束的服务商不会被误改尺寸

---

## 上次（v1.2.0）修的问题

### 你反馈的：导入照片后黑屏
**原因**：手机照片常见 4000×3000 甚至 8000×6000。原来的流程是「先完整解码 → 再缩小」，
仅解码 8000×6000 就要 183MB 连续内存，WebView 渲染进程被系统直接杀掉，界面只剩深色背景，
看起来就是黑屏。

**修复**：改成先只读文件头拿到原始尺寸，再让浏览器在**解码阶段**直接解出工作尺寸的位图，
完全不产生全尺寸图。解码内存峰值从 183MB 降到 27MB（降低约 85%）。
实测 1 亿像素照片也能正常导入。

另外加了两层保险：
- 导入失败时会**直接把原因显示在界面上**（以前是静默黑屏）
- 画布被系统回收时自动恢复，而不是一直黑着

### 你提的三条需求
1. **更新时更新版本号** —— 已做进构建脚本，改动源码后构建会**自动递增版本号**，
   不可能再出现"内容变了但版本号没变"。
2. **覆盖更新保留 API 设置** —— 关键修复：原来本地服务用**随机端口**，而浏览器的
   本地存储是按「地址+端口」隔离的，端口一变，之前存的 API Key 就读不到了。
   现改为固定端口（45871），覆盖安装后设置完整保留。升级后界面会明确提示
   "你的 API 设置已保留"，并可点「更新内容」查看本次改动。
3. **填好地址和 Key 后自动检测可用模型** —— 设置页新增「自动检测可用模型」按钮：
   自动读取模型列表、筛掉对话/语音/向量等非生图模型、标出哪些**支持参考原图编辑**
   （这类才是本应用首选），点一下即可选用。

### 同时修掉的其它问题（经对抗性审查逐项复现确认）
| 问题 | 影响 |
|---|---|
| 贴回位置偏移 | 开启上下文外扩时，内容会整体位移（默认设置下必现） |
| 接缝色彩匹配失效 | 滑块调节完全没效果 |
| 撤销/重做丢效果 | 羽化与画笔保护丢失，出现硬边、覆盖不该改的区域 |
| 生成中换照片 | 旧结果错位写进新照片 |
| 大选区分块有接缝 | 块边界可见色差断层 |
| 细长选区被拉伸 | 画面变形（现按比例裁切贴合） |
| 报错看不懂 | 现在会说明具体原因与解决办法 |
| 小选区拖不动 / 被淡化 | 小选区无法移动；小选区改了却像没改 |
| 布局变化后错位 | 切工具、弹键盘后画布与图像错位 |

---

## 版本历史

| 版本 | versionCode | 说明 |
|---|---|---|
| v1.0.0 | 1 | 首个版本 |
| v1.1.0 | 2 | 增加错误诊断、分块规划等 |
| v1.2.0 | 5 | 修复黑屏；版本号自动递增；覆盖更新保留设置；自动检测模型 |
| v1.3.0 | 6 | 修复 GPT Image 系列尺寸被拒；尺寸错误不再误判；新增 GPT Image 2.5 |
| v1.4.0 | 7 | 修复上游返回文字被误判；生成前预检对话模型；新增查看本次请求 |
| v1.5.0 | 8 | **小选区自动放大后发送**（解决上游最小尺寸限制）；可开关 |
| v1.6.0 | 9 | 修复图片发送格式错误（multipart/JSON），OpenAI 编辑接口真正可用 |
| v1.7.0 | 10 | 修复画笔导致图片偏蓝；发给模型的图片不再带任何标记色 |
| v1.8.0 | 11 | 保留 EXIF 拍摄信息与 ICC 色彩配置；旋转标记正确归一化 |
| v1.9.0 | 12 | 编辑历史内存上限 + 编辑进度自动保存（防崩、防丢工作） |
| v2.0.0 | 13 | 非破坏性编辑：修改记录面板，可调强度/羽化/色彩匹配、可开关、可删除 |
| v2.1.0 | 14 | 统一撤销：画笔、删除、调参、开关全部可撤销/重做 |
| v2.2.0 | 15 | 导出预设：按交付场景一键适配尺寸/质量/元数据，自动移除定位 |
| v2.3.0 | 16 | **成本预估**：生成前显示花费、大额确认、会话累计 |

---

## 使用步骤

1. 安装 `修图台.apk`
2. 打开后点右上角「设置」
3. 「接口地址」填服务商地址（如 `https://api.siliconflow.cn/v1`）
4. 填「API Key」
5. 点「自动检测可用模型」，从列表里选一个标着**可编辑**的模型（推荐 `Qwen/Qwen-Image-Edit`）
6. 回到主界面：打开照片 → 拖动框选要改的位置 → 写下要求 → 点「生成并贴回」
7. 生成完拖动中间竖线对比前后，满意就点「应用」

## 说明
- 照片只发送到你自己填的接口地址，应用本身不做任何上传
- 大照片建议把「工作分辨率上限」设为 2048 或 3072（设置页可调），更省内存也更流畅
