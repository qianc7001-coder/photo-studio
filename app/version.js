/** 由 tools/bump-version.js 从 version.json 自动生成，请勿手改 */
window.PS_VERSION = {
  "versionCode": 29,
  "versionName": "3.2.0",
  "changelog": [
    "删除：大选区自动分块 —— 分块后每块由模型独立生成，重叠区内容必然不一致，加权平均后接缝出现重影/发糊。现在无论选区多大都整块一次生成（成本也从「按块数翻倍」回到固定一次）",
    "新增：工具栏可自由调节 —— 修图时竖向空间最紧张，现在可以拖动底栏手柄把它拉到任意高度，轻点手柄或箭头即可收起/展开；高度会被记住，换图、重启都沿用",
    "新增：工具提示只显示一次 —— 画笔、引导线、自由绘制的说明文字第二次进入不再弹出（按工具名分别记录，将来加新工具时新工具的提示仍会显示一次）",
    "修复：工具提示会「出现又立刻消失」—— 同一帧里被调用两次，第一次记下已读、第二次就判定不该显示，用户根本没看到提示，记录却已经写了",
    "修复：提示重置后仍不显示 —— 只清了存储没清内存缓存",
    "修复：首页收起工具栏依赖一次可能失败的高度测量（测量不到时状态机会判反），改为显式指定收起意图",
    "修复：配置迁移看不到已被移出持久化列表的字段，导致 localStorage 里的分块残留永远清不掉",
    "优化：成本确认文案改为说明「整块选区一次生成」，不再提块数",
    "（延续）引导线自由绘制、首页修改历史、照片信息、导出格式与大小、应用内检查更新、无缝融合、后台保活"
  ]
};
