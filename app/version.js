/** 由 tools/bump-version.js 从 version.json 自动生成，请勿手改 */
window.PS_VERSION = {
  "versionCode": 30,
  "versionName": "3.3.0",
  "changelog": [
    "修复：应用内「检查更新」永远失败 —— 本地代理只会用 POST 转发，而 GitHub 的版本列表用 POST 请求会返回 401 需认证（浏览器打开仓库却一切正常，所以很难察觉）。现在代理支持 GET 转发",
    "修复：GitHub 强制要求 User-Agent 头，代理转发时缺失会被 403 拒绝（浏览器直连会自动带，经代理就没了）",
    "修复：检查更新在代理失败时不会回退直连 —— 生图路径一直有这个回退，检查更新这里漏了，导致代理一有异常整个功能不可用",
    "修复：代理自身报错（200 + __proxyError）被当成成功，导致界面显示「已是最新」而不是「检查失败」，用户无从排查",
    "修复：更新检查的失败退避实际是 16 倍间隔（8 天），与注释写的「最多 4 倍」不符 —— 网络只是短暂故障的用户要等 8 天才重新检查。现在真的是 48 小时封顶",
    "（延续）引导线自由绘制、工具栏高度可调、工具提示只显示一次、首页修改历史、照片信息、导出格式与大小"
  ]
};
