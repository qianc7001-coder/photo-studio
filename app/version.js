/** 由 tools/bump-version.js 从 version.json 自动生成，请勿手改 */
window.PS_VERSION = {
  "versionCode": 33,
  "versionName": "3.4.0",
  "changelog": [
    "修复：按一下返回键就从任何界面直接退出应用 —— 这是个单页应用，设置/修图记录/对比图/历史时间线全是浮层，没有真正的页面跳转，所以 WebView 的 canGoBack() 永远是 false，Android 壳据此把返回键交给了系统。现在返回键改为逐级处理",
    "新增：返回键分层 —— 有浮层先关浮层（作品预览 → 设置/记录/历史/修改记录/照片信息/导出 → 对比图 → 失败说明）；生成中则取消生成；画笔/平移/引导线模式退回框选；编辑中退回首页；已在首页才退出应用",
    "修复：应用里原本没有任何路径能把照片卸下来（S.img 一旦设置就不清空），「回到首页」只能靠退出应用重进。现在返回键可以正常退回首页",
    "注意：退回首页前会先把当前作品存进修图记录，随时可以从首页点回去继续编辑，不会丢修改",
    "（延续）引导线自由绘制、工具栏高度可调、工具提示只显示一次、首页修改历史、照片信息、导出格式与大小、应用内检查更新"
  ]
};
