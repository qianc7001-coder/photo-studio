# 贡献指南

感谢有兴趣改进这个项目。下面是几个关键约定 —— 都是踩过坑之后定下来的。

---

## 快速上手

```bash
git clone <仓库地址>
cd photo-studio

# 跑测试（不需要装任何依赖）
node tools/core-test.js
node tools/regression-test.js

# 端到端测试需要两个可选依赖
npm install --no-save jsdom @napi-rs/canvas
node tools/e2e-test.js

# 代码检查
node tools/lint.js
```

构建见 [README](README.md#从源码构建)。

---

## 代码约定

### 1. 注释要写「为什么」，不是「做了什么」

这个项目的代码里，解释意图的注释比解释行为的注释重要得多。例如：

```js
// ✗ 不好：重复了代码本身
// 把 feather 设成 10
edit.feather = 10;

// ✓ 好：解释了为什么是这个值
// 羽化从边界向内衰减，如果半径超过选区一半，整块都达不到不透明，
// 表现为「改了但永远只改一半」。所以限制在短边的 1/3 以内。
const limit = Math.min(w, h) / 3;
```

### 2. 踩过的坑要留下痕迹

如果某个写法看起来多余但其实是必需的，**必须注释说明**，否则后人会「优化」掉它。

例如 `build-single.js` 里必须用函数形式做替换：

```js
// 注意：必须用「函数形式」做替换。
// 字符串形式的 replace 会把替换串里的 $& / $' / $` / $1 当成特殊标记，
// 而我们的源码里含有 '$' 这样的字符串（例如价格提示 '单价：$'），
// 其中 $' 会被解释成「匹配之后的文本」，把 </body></html> 注入进 JS 字符串。
const put = (from, to) => { html = html.replace(from, () => to); };
```

### 3. 参数不在生成时烧进结果

羽化、色彩匹配、效果强度这些**必须**在合成时应用，不能写进生成结果。
这是「非破坏性编辑」的基础 —— 用户改这些参数时不该重新调用模型（要花钱）。

新增可调参数时，请：
1. 加到 `core.js` 的 `normalizeLayer`
2. 在 `compositeEditInto` 里读取它
3. 在修改记录面板加对应滑块

---

## 测试要求

**修 bug 必须加回归测试。** 这个项目的历史 bug 都很有代表性：

| 缺陷 | 教训 |
|---|---|
| 贴回位置偏移 | 上下文外扩后坐标没换算，默认配置必现 |
| 撤销破坏合成 | 重放时走了硬贴路径，丢了羽化与掩膜 |
| 画笔导致图片偏蓝 | 把标记色画进了发给模型的图 |
| 图片发送格式错误 | Content-Type 被旧值覆盖，图片被静默丢弃 |
| 空估算算成 1 次调用 | 默认值写错，花费统计偏多 |
| 构建产物被污染 | replace 的 `$'` 把 HTML 注入进 JS |

每个都加进了 `tools/regression-test.js`，**改动相关代码时这些测试必须仍然通过**。

### 测试分层

| 文件 | 用途 | 依赖 |
|---|---|---|
| `core-test.js` | 纯逻辑单测 | 无 |
| `regression-test.js` | 已修缺陷的防线 | 无 |
| `e2e-test.js` | 完整流程 + 逐像素校验 | jsdom + @napi-rs/canvas |

端到端测试的价值在于**逐像素校验**：选区外必须零改动、贴回不能偏移、细长选区不能变形。
这类问题靠肉眼审查发现不了。

### 插入测试块请用工具

不要用文本替换直接插入测试块 —— 脚本重复执行会插很多份，导致变量重复声明。

```bash
node tools/insert-block.js tools/e2e-test.js '<块标记>' '<锚点文本>' <块文件>
```

这个工具是幂等的：插入前会先移除同名块。

---

## 提交前检查

```bash
node tools/lint.js          # 必须通过
npm test                    # 单元 + 回归，必须全绿
node tools/e2e-test.js      # 涉及交互的改动必须跑
```

lint 会检查这些（都是真实踩过的坑）：
- 语法错误
- 源码混入 HTML 结构
- 重复的函数定义
- 硬编码密钥
- 版本号不一致
- 调试代码残留

---

## 版本号

**不要手改版本号。** 唯一来源是 `version.json`：

```bash
node tools/bump-version.js
```

它会比对源码指纹：内容变了但版本号没变 → 自动递增；内容未变 → 沿用同号。

构建时 `tools/build-apk.sh` 会自动调用它，并把版本号写进 `AndroidManifest.xml` 和 `app/version.js`。

---

## 不要提交的东西

`.gitignore` 已经处理，但请确认：

- **`android/keystore.jks`** —— 签名密钥，泄露会导致他人能伪造你的更新包
- `dist/` —— 构建产物
- `android/assets/` —— 构建时从 `app/` 复制过来的
- API Key、Token

---

## 涉及外部服务

新增服务商支持时，请在 `core.js` 的 `PROVIDERS` 里加，并注意：

1. **接口地址要对**：OpenAI 系的图像**编辑**是 `/v1/images/edits`（multipart 表单），
   不是 `/v1/images/generations`（JSON）。搞错的话图片会被静默丢弃，
   表现为「上游回一段文字」。
2. **尺寸约束要校验**：不同服务商要求不同（如 OpenAI 要求总像素 ≥ 0.66MP、宽高为 16 的倍数）。
   加到 `SIZE_RULES` 里，让发送前就能拦住。
3. **价格要查证**：加到 `MODEL_PRICES` 时请注明来源，**不要凭印象填**。
   查不到就留空，界面会显示「未知」—— 估错比不估更糟。

---

## 隐私底线

- 照片只能发往**用户自己配置的接口地址**
- 不得添加任何「上传到我们服务器」的行为
- 新增导出预设时，注意默认是否保留 GPS

---

## 提问与反馈

遇到问题时，应用内报错面板可以展开「查看本次请求」，里面有：
接口地址、模型名、发送格式（multipart/JSON）、是否带图、图片大小、出图尺寸。

把这些信息一起提供，能大幅加快定位速度。
