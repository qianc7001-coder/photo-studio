# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

版本号的唯一来源是 `version.json`，构建时会自动写入 APK 与页面。

## v3.1.0（versionCode 28）

- 新增：引导线支持「自由绘制」—— 手画一笔告诉模型头发/水流/衣褶的走向，笔迹会画进发给模型的图片，模型照着走向生成（比文字描述准得多）
- 新增：笔迹颜色可选（红 / 品红 / 青）—— 红发上画红线等于没画，选与画面反差大的颜色；提示词里的颜色名会跟着实际颜色走
- 新增：设置里可关闭「把笔迹画进请求图」—— 万一模型把线条也画进了画面，关掉后笔迹退化成文字说明
- 修复：越界笔迹曾被压到选区边缘形成假直线 —— 分块生成时笔迹常跨出瓦片，夹取后模型会看到一条沿边缘的线
- 修复：笔迹线宽过细，经 JPEG 编码后几乎不可见（实测 600px 图上从 698 个像素掉到 42 个）—— 现在下限 4px，模型能真正看到
- 修复：分块生成时笔迹坐标以瓦片为基准换算，导致第二块之后的笔迹整体偏移甚至消失
- 修复：两端都在框外但贯穿画面的笔迹被整条丢弃（预筛按点判断，改为按包围盒判断）
- 优化：笔迹不参与方向吸附 —— 拉直会毁掉手画的弧度；手画的小抖动会抽稀采样，单笔点数有上限
- 优化：自由绘制有独立的界面提示与配色（和构图线区分开），并说明怎么应对「线被画出来」
- （延续）首页修改历史、照片信息、导出格式与大小、应用内检查更新、无缝融合、后台保活

## v3.0.0（versionCode 27）

- 首页改为「修改历史」：打开应用先看到修过的照片（按日期分组），点一下就能接着改；工具栏默认收起，进入编辑页才展开
- 新增：照片基本信息 —— 一键看清机型、镜头、快门、光圈、ISO、焦距、拍摄时间、尺寸与色彩配置；含 GPS 时只提示不显示坐标
- 新增：引导线 —— 在选区里直接画一条线告诉模型「地平线在这里 / 主体放这儿」，比文字描述准确得多；支持地平线、垂直线、对角线、主体位置四种类型，自动吸附方向，点线即删
- 新增：导出设置面板 —— 可选 JPEG / PNG，长边从 1080 到 4096 或自定义，实时显示输出尺寸与预计体积；只缩不放，避免小图被拉大
- 修复：导出面板选的 PNG 曾被设置页的「自定义」预设覆盖成 JPEG
- 修复：引导线在分块生成时会被压到瓦片边缘，导致模型按错误位置构图
- 优化：手画的引导线会吸附到水平/垂直（12 度内），避免手抖斜角误导模型
- 优化：引导线、画笔、引导线类型选择都会随会话保存，切后台回来不丢
- （延续）应用内检查更新、环境特征提示词、无缝融合、对比图缩放、后台保活、修图记录

## v2.9.0（versionCode 26）

- 新增：应用内检查更新 —— 启动时自动向 GitHub 查询最新版本（每 12 小时最多一次），发现新版本时弹出提示条
- 新增：一键更新 —— 点「立即更新」由系统下载新版本并自动调起安装器，不用手动去网页找
- 新增：可查看该版本的更新内容，也可「忽略此版本」不再提示（出了更新的版本仍会提示）
- 新增：设置里可手动检查更新，并显示上次检查时间与远程最新版本号；也可关闭自动检查
- 修复：GitHub 的 /releases/latest 指向了错误版本 —— 之前补发历史版本时旧版本时间戳变成最新，导致它返回 v2.2.0，用户点「最新版」反而下到更老的包
- 修复：检查更新不再依赖 /releases/latest（它按创建时间判定），改为拉完整列表按版本号挑最高
- 新增：发布脚本会显式声明 latest 并在发布后复核，避免同类问题再次发生
- （延续）环境特征提示词、无缝融合、设置界面系统风格、对比图缩放、后台保活、修图记录

## v2.8.2（versionCode 25）

- 设置界面改成手机系统设置风格：灰色分组标题 + 圆角卡片 + 行间细分隔线 + 右侧值/开关 + 可点行带箭头
- 开关改成拨动样式（原来是普通勾选框）；破坏性操作单独成卡、红色文字
- 设置按功能分成 10 组：生图模型 / 网络 / 贴回质量 / 无缝融合 / 画质与性能 / 后台保活 / 成本预估 / 生成参数 / 隐私 / 关于
- 新增：关于页显示当前版本号，以及三个 GitHub 入口（项目主页、反馈问题、历史版本下载）
- 点击 GitHub 链接会在系统浏览器中打开（应用内不加载外部网页）
- 兼容：所有原有设置项与绑定关系保持完整，老内核下间距仍有兜底
- （延续）环境特征提示词、无缝融合、对比图缩放、后台保活、修图记录、修图历史时间线

## v2.8.1（versionCode 24）

- 新增：每次请求都附带「周围环境特征」描述 —— 生成前先测量选区周围的客观特征（亮度档位、冷暖色调、反差强弱、主光方向），写进提示词，让模型有可对照的目标，而不是只收到一句「请保持一致」
- 为什么有效：模型看不到你照片的像素统计，只说「一致」它不知道该一致成什么样；给出具体特征后它才能照着做
- 光照方向会明确指出（如「主光来自左侧」）—— 方向判断反了会让模型往反方向打光，比不说更糟
- 补充行为约束：不要改变周边参考区域、不要让改动区域边缘出现可见边界、不要添加边框/暗角/光晕、输出要像一次拍摄完成而非后期拼贴
- 与「无缝融合」互补：提示词让模型尽量做对，融合兜住剩下的残差。两者都不额外调用模型（不花钱）
- 可在设置里关闭（设置 → 贴回质量 → 每次请求附带环境特征）
- 修复：光照方向判断写反了 —— 「左亮右暗」被描述成「主光来自右侧」，会让模型往完全相反的方向打光
- （延续）无缝融合、对比图缩放、后台保活、修图记录、分块默认关闭、修图历史时间线、成本预估

## v2.8.0（versionCode 23）

- 新增：无缝融合 —— 针对「生成结果和周围环境不契合」的根本手段。模型看不到你照片的明暗走向和质感，只能靠提示词猜，所以总差一点；现在在贴回时强制对齐
- 新增：光照渐变对齐 —— 拟合周围环境的光照走向（如「左亮右暗」），把生成块按同样梯度调整。这是「一眼看出贴过」最常见的原因：原图有明暗渐变，生成块却是平的
- 新增：对比度对齐 —— 模型出图常偏灰或过艳，按周围环境的明暗起伏程度自动校正
- 新增：颗粒补偿 —— 模型输出比真实照片平滑，在接缝附近补上相当强度的颗粒，避免「那块太干净」
- 新增：契合度评分 —— 每个图层显示 0~100 分和具体问题（色差／亮度台阶／对比度不一致），调参数时能看出有没有变好
- 新增：图层级「无缝融合」滑块 + 设置里的全局强度／中心保留／颗粒补偿，全部即时生效、不重新调用模型（不花钱）
- 关键设计：中心区域不做色调对齐 —— 你改成的颜色会被完整保留，融合只修接缝附近的技术性瑕疵，不跟你的修改意图对着干
- 修复：接缝契合度对纯色块误报「对比度不一致」（标准差为 0 时算出了 100% 差异）
- （延续）对比图缩放、后台保活、修图记录、分块默认关闭、修图历史时间线、成本预估、导出预设

## v2.7.0（versionCode 22）

- 修复：对比图无法放大 —— 提示条写着「双击画面放大查看」但双击一直不生效，因为按下即拖分割线，两次点击各把线拽到了手指位置
- 新增：双击放大查看 —— 双击画面以点击处为中心放大（点哪里就看哪里），再次双击还原；放大后单指拖动平移，双指捏合缩放
- 新增：放大后仍可拖动分割线（按在竖线附近即可），不影响对比；分割线会自动保持在视口内，不会跑到看不见的地方
- 新增：放大后显示倍数角标与「还原」按钮，不用记手势也能回到全图；提示语会随状态切换
- 修复：放大后无法再双击还原 —— 单指在放大态是平移，导致「按下没动」也被当成拖动，用户会被卡在放大态出不去
- 修复：退出对比视图时没有清掉缩放状态，下次生成会带着上次的缩放和偏移进来
- 优化：小图放大时至少铺满视口 —— 只按固定倍数放大的话，小图放大后四周仍是空白，看不出变化
- （延续）后台保活、修图记录、分块默认关闭、修图历史时间线、成本预估、导出预设、统一撤销、非破坏性编辑

## v2.6.0（versionCode 21）

- 新增：后台保活 —— 生图请求要 30~60 秒，这期间切到别的应用或锁屏，系统会回收进程导致请求中断（模型已出图、钱已花），现在生成期间会自动开启前台服务把进程钉住，切走也能拿到结果
- 新增：生成完成通知 —— 切走或锁屏时，出图后会发一条通知告诉你「好了」，不用反复切回来查看；失败也会通知
- 新增：可选的「一直保活」开关，连续修图时不用每次等系统调度；代价是状态栏常驻一条通知
- 新增：电池优化白名单引导 —— 各家手机系统（小米/华为/OPPO/vivo/三星…）后台限制不同，设置里可一键跳转到系统授权页
- 兼容：适配 Android 5.0 ~ 14（API 21~34）与各厂商定制系统，所有系统接口都按版本分流，老设备走降级分支而不是崩溃
- 兼容：修复 Android 5/6 上点击外链无反应（只重载了新版的 URL 拦截方法，老系统走的是旧签名）
- 兼容：修复旧版浏览器内核下界面错位 —— 运行时实测 flex gap 等特性，不支持时自动打补丁（不能只用 @supports 判断，Chrome 66~83 会误报为支持）
- 兼容：通知图标按各屏幕密度生成（此前缺失，发通知会崩），并修正为系统要求的纯白剪影
- （延续）修图记录、分块默认关闭、修图历史时间线、成本预估、导出预设、统一撤销、非破坏性编辑、会话恢复、EXIF/ICC 保留

## v2.5.0（versionCode 19）

- 新增：修图记录（跨天作品库）—— 修过的每一张照片都会自动留档，关掉应用、第二天打开仍然在，按「今天 / 昨天 / 具体日期」分组，点开可看大图
- 每条记录带缩略图、文件名、修改处数、当时时刻；只要编辑数据还在，就能「继续编辑这张」，直接回到当时的状态接着改
- 同一张照片反复修改只占一条记录（原地更新），不会每改一次就多一条
- 空间不足时先丢较早记录的编辑数据（只保留预览图），再清理最早的记录，尽量多留可见历史；清理时会明确提示
- 修复：作品库体积预算算错 —— 降级（丢会话）后又按完整体积扣一次，账面占用远小于真实占用，会导致写爆存储配额
- 修复：时间戳损坏的记录会显示成「00:00」并出现没有标题的分组，现在分别显示为空和「时间未知」
- 修复：「继续编辑」恢复作品时没有作废在途的生成请求，旧请求返回后会把结果贴到刚恢复的照片上
- 优化：会话与作品库打包结果加缓存，避免一次编辑把整张画布编码两遍（大图下每次可省几百毫秒）
- （延续）分块默认关闭、修图历史时间线、成本预估、导出预设、统一撤销、非破坏性编辑、会话恢复、EXIF/ICC 保留

## v2.4.1（versionCode 18）

- 关闭：大选区自动分块 —— 分块后每块由模型独立生成，重叠区内容必然不完全一致，加权平均会在接缝处出现重影或发糊。这是分块方案本身的固有缺陷，调参数治不好，所以默认关掉
- 选区再大也是整块一次发送，接缝问题从根上消失（代价是单次请求的图片更大）
- 设置里仍保留这个开关：万一遇到服务商拒绝大尺寸请求，可以临时打开；界面已注明风险
- 已安装的用户升级后会自动关闭（原来的开启状态会被改掉），升级提示里会说明这次改了什么
- 修复：配置迁移只改了内存、没有写回存储，导致每次启动都重新迁移一遍 —— 用户手动改回来的设置会被反复覆盖
- 修复：脏配置值（如字符串形式的数字）会绕过迁移判断，把非法值留在配置里
- 新增：配置迁移测试（14 项），模拟「老用户升级」的真实路径，验证设置真的被关掉、且只关一次
- （延续）修图历史、成本预估、导出预设、统一撤销、非破坏性编辑、会话恢复、EXIF/ICC 保留




## v2.4.0（versionCode 17）

- 新增：修图历史 —— 工具栏「历史」按钮打开时间线，按顺序列出你对这张照片做过的每一步操作（生成、画笔、调参、删除都有）
- 新增：历史预览 —— 点时间线上任意一步，画面会真的回到那一刻，可以先看效果再决定要不要退回去
- 新增：一键跳回 —— 确认后即可回到选中的那一步，后面的步骤会被撤销（也能再跳回来，直到你确认）
- 新增：每步都有摘要 —— 显示这一步改了什么（如「羽化：0px → 12px」「选区 300×200」）和当时有几处修改
- 历史不存图片快照，只复用撤销记录，因此 100 步历史几乎不占额外内存
- 修复：内存整理丢弃最老的修改时，没有同步修正撤销记录里的位置，会导致撤销删掉错误的图层（这个 bug 只在历史超过内存上限后才会触发，很难遇到）
- （延续）成本预估、导出预设、统一撤销、非破坏性编辑、会话恢复、EXIF/ICC 保留

## v2.3.0（versionCode 16）

- 新增：成本预估 —— 生成前显示这次要花多少钱（美元 + 人民币参考价）
- 新增：自动计入分块开销 —— 选区过大时会分块处理，每块各调用一次模型，成本按次数累加并提前显示
- 新增：大额确认 —— 预估超过 0.2 美元时先弹确认，避免误操作烧钱（含分块原因说明）
- 新增：会话累计花费 —— 模型按钮上显示本次已花金额与调用次数
- 新增：可自定义单价与汇率（设置里填写），也支持给未收录的模型手动填单价
- 价格来源为各服务商官方页面/博客（硅基流动、OpenAI），未收录的模型显示「未知」而非编造数字
- 修复：空估算被当成 1 次调用，导致花费统计算多
- （延续）导出预设、统一撤销、非破坏性编辑、内存上限、会话恢复、EXIF/ICC 保留
